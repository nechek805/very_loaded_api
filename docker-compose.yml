services:
  api:
    build: .
    ports:
      - "8000:8000"
    volumes:
      - .:/app
    user: "1000:1000"
    environment:
      HOME: /tmp
      UV_CACHE_DIR: /tmp/uv-cache
    command: uv run uvicorn src.main:app --host 0.0.0.0 --port 8000